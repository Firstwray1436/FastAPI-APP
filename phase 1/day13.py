"""🔵 Day 13 — POST + Pydantic
Goal

Build a FastAPI endpoint that accepts JSON, validates it using a Pydantic model, and shows validation errors automatically in Swagger.

FastAPI uses Pydantic models to declare request bodies and validate incoming data.

Learn
1. Request body

A POST request can send JSON data in the body.

Example JSON:


"""
